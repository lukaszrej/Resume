language: node_js
node_js:
  - 10.15.3

cache: npm

install:
  - npm ci

script:
  - npm test
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
